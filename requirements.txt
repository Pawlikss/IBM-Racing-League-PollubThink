# Python 3.10+.
# Versions use compatible ranges to keep setup flexible across machines.

optuna>=3.0,<5.0
pyautogui>=0.9,<1.0
numpy>=1.20,<3.0

# Optional: browser dashboard for Optuna studies.
# optuna-dashboard>=0.14
